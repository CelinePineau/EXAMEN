<h1>TOUS LES HOTELS</h1>
<div style="height: 100px"></div>
<div class="row">
  <% @hotels.each do |hotel| %>
    <div class="col-md-4">
      <%= link_to hotel.title, hotel_path(hotel), class: "btn btn-menu" %>
      <div style="height: 50px"></div>
    </div>
  <% end %>
</div>
<div style="height: 100px"></div>
<%= link_to "Retour à l'Accueil", root_path %>
<%= link_to "Ajouter un nouvel hotel", new_hotel_path(@hotel) %>
